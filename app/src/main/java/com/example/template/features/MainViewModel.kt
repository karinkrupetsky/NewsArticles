package com.example.template.features

import androidx.lifecycle.ViewModel
import com.example.template.repositories.MainRepository

class MainViewModel(private val repository: MainRepository): ViewModel() {
}